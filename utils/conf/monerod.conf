# Configuration for chutokend
# Syntax: any command line option may be specified as 'clioptionname=value'.
# See 'chutokend --help' for all available options.

data-dir=/var/lib/monero
log-file=/var/log/monero/monero.log
log-level=0
